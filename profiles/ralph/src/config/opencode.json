{
  "permissions": {
    "read": {
      "deny": ["**/.ssh/**", "**/*.pem", "**/aws/**", "**/.env", "**/*.key"]
    },
    "edit": {
      "deny": ["**/.ssh/**", "**/*.pem", "**/aws/**", "**/.env", "**/*.key"]
    },
    "bash": {
      "deny": ["wget", "curl -o", "curl --output"]
    }
  }
}
